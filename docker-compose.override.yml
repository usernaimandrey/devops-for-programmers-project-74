---
version: '3.2'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # extends:
    #   file: docker-compose.yml
    #   service: app
    command: npm run dev
    volumes:
      - ./app:/app
    ports:
      - 8080:8080
